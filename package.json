{
  "private": true,
  "scripts": {
    "build": "webpack",
    "serve": "browser-sync start -s public -w --no-open --no-notify --no-ghost-mode"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "html-webpack-plugin": "^4.5.1",
    "webpack": "^5.17.0",
    "webpack-cli": "^4.4.0"
  }
}
