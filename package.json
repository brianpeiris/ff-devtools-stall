{
  "private": true,
  "scripts": {
    "build": "webpack",
    "serve": "browser-sync start -s dist -w --no-open --no-notify --no-ghost-mode"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "html-webpack-plugin": "^4.2.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.2.3"
  }
}
